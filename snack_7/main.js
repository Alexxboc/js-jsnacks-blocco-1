/*
Stampa le potenze di 2 fino a 1000.
*/

// Creo una variante per il numero da utilizzare nell'operazione

const number = 2;

// Creo un ciclo di 1000 iterazioni
for(let i = 1; i < 1000 + 1; i++){

// Stampo in console le potenze di 2 fino a 1000
    console.log(Math.pow(number,i))
    
}